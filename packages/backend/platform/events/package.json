{
  "name": "@iam/platform-events",
  "version": "0.1.0",
  "private": true,
  "description": "Transactional outbox, CloudEvents transport, delivery, retry, and idempotency primitives.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
