{
  "name": "@iam/platform-config",
  "version": "0.1.0",
  "private": true,
  "description": "Fail-fast typed runtime configuration and redacted startup summaries.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
