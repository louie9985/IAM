# @iam/reference-app

最小 NestJS + React 接入示例，后续作为登录、BFF、权限 Guard、数据范围、应用内角色管理、即时收权、会话吊销和审计上报的可执行文档。

当前 M0 Task 0.3 只登记 Workspace 清单，不创建源码或伪造集成结果。参考应用将在相关 SDK 和协议能力可真实验证后按切片实现。

参考应用只能使用公开协议、`@iam/contracts` 和公开 SDK，禁止连接 IAM 数据库或导入内部领域/平台包。
